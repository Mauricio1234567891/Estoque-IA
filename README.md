# Estoque IA V12 — Deploy pela raiz

Versão preparada para upload pelo celular e deploy no Render.

Build: `npm install`
Start: `npm start`

Variáveis: `DATABASE_URL`, `JWT_SECRET`, `NODE_ENV=production`.
